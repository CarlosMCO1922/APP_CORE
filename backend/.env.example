# Backend CORE - Variáveis de ambiente (cópia para .env e preencher valores)
# Nunca commitar o ficheiro .env com dados sensíveis.

# Servidor
NODE_ENV=development
PORT=3001

# Base de dados (PostgreSQL em produção; SQLite/outro em dev)
# Produção: usar DATABASE_URL. Desenvolvimento: ver config/database.js
DATABASE_URL=postgresql://user:password@host:5432/dbname

# CORS - origens permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:3000,https://app-core-frontend-wdvl.onrender.com

# Autenticação JWT
JWT_SECRET=your-secret-key-min-32-chars

# Frontend (para links em emails e redirects)
FRONTEND_URL=http://localhost:3000

# Email (Nodemailer) - pedidos públicos, confirmações, alertas
# Conta de envio: core.reab@gmail.com (definir SMTP_PASS no .env local; nunca commitar)
# Gmail: usar "Palavra-passe de aplicação" se tiver 2FA ativado
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=core.reab@gmail.com
SMTP_PASS=
FROM_EMAIL=

# Alertas de erro crítico (emails para admins, separados por vírgula)
ADMIN_ALERT_EMAILS=

# Stripe (pagamentos) - opcional
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=

# Sentry (monitorização de erros) - opcional
# SENTRY_DSN=
# SENTRY_TRACES_SAMPLE_RATE=0.1

# Retenção de logs (dias)
LOGS_RETENTION_DAYS=90

# Gabinete partilhado: IDs de staff que usam o mesmo espaço físico (ex.: Elsa + Inês Soares).
# Quando um tem consulta, os horários ficam bloqueados para os outros. Valores separados por vírgula (ex.: 2,5).
# Deixar vazio se não houver partilha de gabinete.
# SHARED_OFFICE_STAFF_IDS=2,5
